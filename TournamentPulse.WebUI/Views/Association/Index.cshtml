@using TournamentPulse.WebUI.Models.Association;
@model List<AssociationListViewModel>

<!DOCTYPE html>
<html>
<head>
    <title>List of Associations</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <h1>@_localization.Getkey("asp_ListOfAssociations")</h1>

    <a href="@Url.Action("Add", "Association")" class="btn btn-success mb-3">@_localization.Getkey("asp_AddNewAssociation")</a>

    @if (Model.Any())
    {
        <div class="container">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>@_localization.Getkey("acp_Name")</th>
                        <th>@_localization.Getkey("asp_Fighters")</th>
                        <th>@_localization.Getkey("asp_Academies")</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var association in Model)
                    {
                        <tr>
                            <td><a class="nav-link" href="@Url.Action("Detail", "Association", new { associationName = association.Name })"/>@association.Name</td>
                            <td>@association.FightersCnt</td>
                            <td>@association.AcademiesCnt</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <p>No Associations found.</p>
    }

    <!-- Include Bootstrap JS (optional) -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
